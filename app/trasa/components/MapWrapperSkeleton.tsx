import React from "react";
import { Skeleton } from "@/components/ui/skeleton";

const MapWrapperSkeleton = () => {
  return <Skeleton className="w-full h-[400px]" />;
};

export default MapWrapperSkeleton;
